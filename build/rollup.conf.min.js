import nodeResolve from 'rollup-plugin-node-resolve';
import babel from 'rollup-plugin-babel';
import { terser } from 'rollup-plugin-terser';


export default {
  input: 'src/index.js',
  plugins: [
    nodeResolve(),
    babel(),
    terser(), // uglifyjs alternative with es6 support
  ],
  output: {
    format: 'umd',
    name: 'VueMaskRu',
    exports: 'named',
    file: 'dist/v-mask-ru.min.js',
  },
};
